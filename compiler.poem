include "./tokenizer.poem"
include "bnny/bnny.poem" bnny\

~ filesystem callbacks
var read
var list
var finish

var error

func compile filename callback
  finish = callback
  #read filename #load_file

func load_file success data reqId
  if success
    #finish (string_upper data)
  else
    error = "sorry, sir! I just couldn't do it!"
    #finish null

